services:
  - type: web
    name: backend-iot-sentinel
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app
    envVars:
      - key: PORT
        value: 10000
```

**Fix 3 — Add `gunicorn` to `requirements.txt`:**
```
flask
flask-cors
scikit-learn
numpy
pandas
joblib
scipy
gunicorn